/**
 * Creates a Set from an input array.
 * @param {Array} array - The input array (can contain any data type)
 * @returns {Set} - A Set containing unique values from the array
 */
export default function setFromArray(array) {
  return new Set(array);
}
